{
  "name": "orbital-station",
  "version": "0.1.0",
  "description": "Prototipo: defensa orbital de una estacion espacial modular (Phaser 3)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --import ./test/setup.js --test \"test/**/*.test.js\"",
    "test:cov": "node --import ./test/setup.js --test --experimental-test-coverage \"test/**/*.test.js\""
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
